# Actually portable Zen-C

## Development

After clonning the repo with submodules, run the following commands to download required tools and perform build

```bash
./boot-repo.com    # download unzip, zip, cosmocc into ./usr/bin
usr/bin/make       # build zc.com
```